  <%= form_with(model:[@post, @comment],remote:true)do |f| %>
  <%= f.text_area :comment, placeholder: "コメントをここに",style:"width:300px; height:150px; resize:none;"%><br>
  <%= f.submit "送信する",class: 'btn btn-success' %>
  <% end %>