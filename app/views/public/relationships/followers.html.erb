<div class="container">
  <div class="row d-flex justify-content-center">
    <div class="col-lg-6">
      <h4 class="mb-4 text-center"><i class="fas fa-user-friends"></i>
        <% if @user == current_user %>
          Myフォロー
        <% else %>
          <%= @user.name %> さんがフォローしている人
        <% end %>
        (<%= @users.count %>人)
      </h4>
    </div>
  </div>
</div>